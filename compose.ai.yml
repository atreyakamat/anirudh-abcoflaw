version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: law_crm_ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - law_crm_network

volumes:
  ollama_data:
    driver: local
