version: '3.8'

services:
  backend:
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug
    ports:
      - "3001:3001"

  frontend:
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
